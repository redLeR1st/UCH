using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToolsController : MonoBehaviour {

	public GameObject tool;
	private GameObject newTool;
	private GameObject newTool2;

	// Use this for initialization
	void Start () {
	/*	newTool = Instantiate(tool, new Vector3(0.5f, 0.5f, 0.0f), tool.transform.rotation) as GameObject;
		newTool.SetActive(true);*/

	}
	
	// Update is called once per frame
	void Update () {
	/*	if (Input.GetButtonDown("Fire1"))
		{

			
			newTool2 = Instantiate(tool, new Vector3(0f, 2.5f, 0.0f), tool.transform.rotation);

			newTool2.SetActive(true);
		}*/
	}
}
